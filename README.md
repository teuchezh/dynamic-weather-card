# Animated Weather Card для Home Assistant

Красивая анимированная карточка погоды в стиле iOS с плавными анимациями для Home Assistant.

## Особенности

- 🌞 **Анимированное солнце** с вращающимися лучами и ореолом
- 🌙 **Луна и звезды** с эффектом мерцания
- ☁️ **Облака** с плавным движением и различной плотностью
- 🌧️ **Дождь** с реалистичными каплями
- ❄️ **Снег** с красивыми снежинками, вращающимися при падении
- 🌫️ **Туман** с многослойными эффектами
- ⚡ **Гроза** с эффектом молнии и свечением
- 🌅 **Восход/Закат** с реалистичными эффектами горизонта
- 🌓 **Автоматическая смена дня/ночи** в зависимости от времени суток
- 🎨 **Стиль iOS** с плавными градиентами и переходами

## Установка

### Способ 1: HACS (рекомендуется)

1. Установите [HACS](https://hacs.xyz/) если еще не установлен
2. Перейдите в HACS → Frontend
3. Нажмите кнопку с тремя точками в правом верхнем углу
4. Выберите "Custom repositories"
5. Добавьте этот репозиторий
6. Найдите "Animated Weather Card" и установите

### Способ 2: Ручная установка

1. Скачайте файл `animated-weather-card.js`
2. Поместите его в папку `www/community/animated-weather-card/` вашего Home Assistant
3. Добавьте в `configuration.yaml`:

```yaml
lovelace:
  resources:
    - url: /local/community/animated-weather-card/animated-weather-card.js
      type: module
```

4. Перезагрузите Home Assistant

## Конфигурация

Добавьте карточку в ваш Lovelace дашборд:

```yaml
type: custom:animated-weather-card
entity: weather.home
```

### Параметры

| Параметр | Тип | Обязательный | Описание | По умолчанию |
|----------|-----|--------------|----------|--------------|
| `entity` | string | Да | ID сущности погоды | - |
| `name` | string | Нет | Имя для отображения | Из атрибутов сущности |
| `height` | number | Нет | Высота карточки в пикселях | 200px |
| `show_feels_like` | boolean | Нет | Показывать температуру "ощущается как" | true |
| `show_wind` | boolean | Нет | Показывать скорость ветра | true |
| `show_wind_direction` | boolean | Нет | Показывать направление ветра | true |
| `show_wind_gust` | boolean | Нет | Показывать порывы ветра | true |
| `show_humidity` | boolean | Нет | Показывать влажность | true |
| `show_min_temp` | boolean | Нет | Показывать минимальную температуру под текущей | true |
| `show_forecast` | boolean | Нет | Показывать прогноз на сегодня (автоматически увеличивает высоту карточки) | false |
| `icons_path` | string | Нет | Путь к папке с иконками (если используются SVG) | `/local/community/animated-weather-card/icons/` |

### Пример конфигурации

```yaml
type: custom:animated-weather-card
entity: weather.openweathermap
name: Погода в Москве
height: 250
show_feels_like: true
show_wind: true
show_wind_direction: true
show_wind_gust: true
show_humidity: true
show_min_temp: true
```

### Минимальная конфигурация (только основные элементы)

```yaml
type: custom:animated-weather-card
entity: weather.openweathermap
show_feels_like: false
show_wind: false
show_humidity: false
show_min_temp: false
```

## Поддерживаемые погодные условия

Карточка автоматически определяет текущее состояние погоды и отображает соответствующую анимацию:

- `sunny` / `clear` - Солнечно
- `cloudy` - Облачно
- `partlycloudy` - Переменная облачность
- `rainy` / `rain` - Дождь
- `snowy` / `snow` - Снег
- `foggy` / `fog` - Туман
- `lightning` / `lightning-rainy` - Гроза

## Время суток

Карточка автоматически меняет фон и анимации в зависимости от времени суток:

- **6:00 - 8:00** - Восход солнца 🌅
- **8:00 - 18:00** - День ☀️
- **18:00 - 20:00** - Закат 🌇
- **20:00 - 6:00** - Ночь 🌙

## Совместимость

Работает со всеми стандартными интеграциями погоды Home Assistant:
- OpenWeatherMap
- Met.no
- Dark Sky (если еще используется)
- И другие...

## Разработка

Для разработки локально:

```bash
npm install
npm run build
```

## Лицензия

MIT License

## Автор

Создано с ❤️ для сообщества Home Assistant

